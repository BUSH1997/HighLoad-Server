worker_processes 8;
events {}

http {
    default_type  application/octet-stream;

    sendfile        on;

    server {
        listen       9090;
        server_name  nginx_localhost;
        root /Users/s.kust/TP/HighLoad-Server;
    }
}
